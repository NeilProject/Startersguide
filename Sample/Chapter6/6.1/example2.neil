Init
	table zoo
	zoo += "Lion"
	zoo += "Monkey"
	zoo += "Rhino"
	zoo += "Stork"
	zoo += "Crocodile"
	zoo += "Bat"
	zoo += "Snake"
	zoo += "Shark"
	zoo += "Pinguin"
	
	for i=1,#zoo
		print(zoo[i])
	end
end
