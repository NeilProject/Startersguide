Init
	table zoo
	zoo += "Lion"
	zoo += "Monkey"
	zoo += "Rhino"
	zoo += "Stork"
	zoo += "Crocodile"
	zoo += "Bat"
	zoo += "Snake"
	zoo += "Shark"
	zoo += "Pinguin"
	
	for idx,animal in ipairs(zoo)
		print(idx,animal)
	end
end
